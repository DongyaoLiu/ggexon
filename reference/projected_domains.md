# Access stored projected protein-domain tables

Returns the contents of the `projected_domains` slot on a
`SynIndividual`.

## Usage

``` r
projected_domains(x)
```

## Arguments

- x:

  A `SynIndividual` object.

## Value

A named list of stored projected protein-domain tables.
