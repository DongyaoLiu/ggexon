# Extract a built grob for a layer

`layer_grob` is an alias of `get_layer_grob()`.

## Usage

``` r
layer_grob(plot = get_last_plot(), i = 1L)
```

## Arguments

- plot:

  A built ggplot or ggexon object.

- i:

  Layer index.
