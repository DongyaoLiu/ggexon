# Set the active feature annotation layer on a SynIndividual

Set the active feature annotation layer on a SynIndividual

## Usage

``` r
set_active_feature_annotation(x, name)
```

## Arguments

- x:

  A `SynIndividual` object.

- name:

  Feature annotation layer name.

## Value

An updated `SynIndividual` object.
