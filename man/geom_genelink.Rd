% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_genelink.R
\name{geom_genelink}
\alias{geom_genelink}
\title{Draw link lines between pairs of coordinates}
\usage{
geom_genelink(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  link_type = "straight",
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
Required: \code{x}, \code{y}, \code{xend}, \code{yend}.}

\item{data}{The data to be displayed.}

\item{stat, position, ..., na.rm, show.legend, inherit.aes}{Standard ggplot2
layer arguments.}

\item{link_type}{Line style: \code{"straight"}, \code{"elbow"}, or \code{"spline"}.
Default \code{"straight"}.}
}
\value{
A ggplot2 layer using the internal \code{GeomGeneLink} ggproto.
}
\description{
\code{geom_genelink()} draws link lines between start points \verb{(x, y)} and end
points \verb{(xend, yend)}. Three line styles are supported via the
\code{link_type} parameter:
}
\details{
\itemize{
\item \code{"straight"} (default) — a single straight segment.
\item \code{"elbow"} — two right-angle segments with a bend at \verb{(x, yend)}.
\item \code{"spline"} — a smooth cubic Bézier curve from \verb{(x, y)} to
\verb{(xend, yend)} with auto-derived control points.
}

This geom is designed to work generically with any coordinate data,
making it reusable for gene labels, mutation annotations, or other
link-line needs.
}
