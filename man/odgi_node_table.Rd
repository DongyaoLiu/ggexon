% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odgi.R
\name{odgi_node_table}
\alias{odgi_node_table}
\title{Build a node-by-node table from an ODGI graph}
\usage{
odgi_node_table(
  og_file,
  output = NULL,
  odgi = NULL,
  python = NULL,
  read = TRUE
)
}
\arguments{
\item{og_file}{Path to the input ODGI graph (\code{.og}).}

\item{output}{Optional output TSV path. Defaults to \verb{<graph>.node_table.tsv}
next to \code{og_file}.}

\item{odgi}{Optional path to the \code{odgi} executable. If omitted, the helper
falls back to \code{ODGI_BIN} and then \code{odgi} on \code{PATH}.}

\item{python}{Optional path to the Python interpreter. Defaults to
\code{python3}, then \code{python}, on \code{PATH}.}

\item{read}{Logical; if \code{TRUE}, read and return the generated TSV as a data
frame. If \code{FALSE}, return the output path.}
}
\value{
A data frame when \code{read = TRUE}, otherwise the output file path.
}
\description{
Runs the bundled Python helper \code{odgi_node_table.py} against an \code{.og} graph
and returns either the generated TSV path or the parsed table.
}
\examples{
\dontrun{
tbl <- odgi_node_table("graph.og")

path <- odgi_node_table(
  "graph.og",
  output = "graph.node_table.tsv",
  read = FALSE
)
}
}
