% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/syn-layer.R
\name{project_mutations_to_genome}
\alias{project_mutations_to_genome}
\title{Project amino-acid variants onto genomic coordinates}
\usage{
project_mutations_to_genome(
  x,
  annotation = NULL,
  genes = NULL,
  transcripts = NULL,
  strains = NULL,
  mutation = NULL,
  event_type = NULL,
  min_sample_count = NULL,
  protein_ranges = NULL,
  ref = NULL,
  chr = NULL,
  start = NULL,
  end = NULL
)
}
\arguments{
\item{x}{A \code{SynIndividual} object.}

\item{annotation}{Optional \code{SynProteinMutationAnnotation} layer name.
Defaults to the first attached protein-mutation annotation.}

\item{genes, transcripts}{Optional identifiers limiting the transcripts that
variants are projected onto.}

\item{strains, mutation, event_type, min_sample_count, protein_ranges, ref}{Optional variant filters forwarded to \code{\link[=query_protein_mutations]{query_protein_mutations()}}.}

\item{chr, start, end}{Optional genomic window used to clip the projection.}
}
\value{
A data frame with one row per (variant, overlapped CDS segment),
containing the projected \code{seqnames}, \code{xmin}, \code{xmax}, \code{strand},
\code{transcripts}, and the variant metadata columns (\code{position}, \code{ref}, \code{alt},
\code{mutation}, ...). Returns an empty data frame when nothing projects.
}
\description{
Converts protein-coordinate variants (for example \code{C316H} at residue 316)
into genomic coordinates using a transcript's CDS structure, so amino-acid
variants can be annotated directly on the exon/intron model drawn by
\code{\link[=geom_exon]{geom_exon()}}. Each variant is treated as a single codon: residue \code{p} maps to
CDS nucleotides \verb{(p - 1) * 3 + 1 .. p * 3}, walked across CDS segments so a
codon that spans a splice junction yields one genomic row per segment. The
phase of the 5'-most CDS is honoured for 5'-truncated gene models.
}
\details{
This is the mutation counterpart of \code{\link[=project_domains_to_genome]{project_domains_to_genome()}} and shares
the same coordinate-projection core.
}
\seealso{
\code{\link[=project_domains_to_genome]{project_domains_to_genome()}}, \code{\link[=geom_aa_variant]{geom_aa_variant()}}
}
