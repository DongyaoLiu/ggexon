% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-exon.R
\name{geom_exon}
\alias{geom_exon}
\title{Draw exon-style genomic features}
\usage{
geom_exon(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  transcripts_track_ratio = NULL,
  exon_height = NULL,
  transcript_backbone_ratio = NULL,
  transcript_backbone_fill = NULL,
  transcript_backbone_colour = NULL,
  x_translation = NULL,
  subset = NULL,
  annotation_type = "exon",
  species = NULL,
  chr = NULL,
  breakdata = NULL,
  force_flat = FALSE,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
In addition to the required positional aesthetics, Syn-backed exon layers
expose canonical identifier columns \code{transcript_id}, \code{gene_id}, and
\code{gene_name} for use in aesthetic mappings.}

\item{data}{A data frame, \code{SynSpecies}, or \code{SynIndividual} object.}

\item{stat, position}{Standard ggplot2 layer arguments.}

\item{...}{Additional parameters passed on to the layer, including fixed
aesthetics such as \code{fill}, \code{colour}, and \code{alpha}.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a
warning. If \code{TRUE}, missing values are removed silently.}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{transcripts_track_ratio}{Optional transcript track ratio used by the
ggexon layout helpers.}

\item{exon_height}{Optional exon rectangle height.}

\item{transcript_backbone_ratio}{Relative backbone height as a fraction of
\code{exon_height}. Defaults to \code{0.1}.}

\item{transcript_backbone_fill, transcript_backbone_colour}{Optional fixed
fill and outline for transcript backbones and direction indicators. When
\code{NULL}, ggexon preserves the existing inherited/default backbone
aesthetics for backwards compatibility.}

\item{x_translation}{Optional x offset applied before drawing.}

\item{subset}{Optional numeric length-2 genomic window to keep. When omitted
for Syn-backed data, the full annotation range is used.}

\item{annotation_type}{Feature type to keep, defaults to \code{"exon"}.}

\item{species}{Optional species / individual identifier when \code{data} is a
\code{SynSpecies}. When omitted, ggexon resolves exon data for all stored
individuals.}

\item{chr}{Optional chromosome / seqname restriction when \code{data} is Syn-backed.}

\item{breakdata}{Optional break specification passed to \code{addbreak()}.}

\item{force_flat}{When \code{TRUE}, draw every gene on one shared y-row instead of
vertically staggering overlapping genes.}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics rather than
combining with them.}
}
\value{
A ggplot2 layer using \code{\link{GeomExon}}.
}
\description{
\code{geom_exon()} draws exon-like genomic intervals as filled rectangles with a
transcript backbone and direction indicator. It supports ordinary ggplot2
aesthetic mappings and can resolve \code{SynSpecies} / \code{SynIndividual} inputs
lazily during plot build.
}
\details{
For Syn-backed layers, ggexon adds canonical identifier columns to the
resolved exon table so users can map aesthetics with expressions such as
\code{aes(fill = gene_id)}, \code{aes(fill = gene_name)}, or
\code{aes(fill = transcript_id)}.

When \code{data} is a \code{SynSpecies}, omitting \code{species} uses all stored
individuals. Omitting \code{subset} keeps the full annotation table, while
supplying \code{chr} without \code{subset} limits the layer to that seqname.
}
