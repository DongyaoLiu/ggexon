% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odgi.R
\name{odgi_species_order}
\alias{odgi_species_order}
\title{Derive a greedy species order from an ODGI multiple alignment}
\usage{
odgi_species_order(
  x,
  reference_species,
  selected_species = NULL,
  alignment = NULL,
  filter_by_len = NULL,
  individuals = NULL,
  odgi = NULL,
  python = NULL
)
}
\arguments{
\item{x}{A \code{\link{SynMultiAlignment}} with \code{format = "odgi"}, a \code{\link{SynSpecies}}
object containing an ODGI multiple alignment, an ODGI node-table data
frame, a path to an ODGI node-table TSV, or a raw \code{.og} graph path.}

\item{reference_species}{Reference species that seeds the greedy walk.}

\item{selected_species}{Optional subset of species to include. Species not
present in the ODGI alignment are ignored.}

\item{alignment}{Optional alignment name when \code{x} is a \code{\link{SynSpecies}} with
multiple stored multiple-alignments.}

\item{filter_by_len}{Optional ODGI node-length filter such as \code{"> 10"} or
\code{"<= 3"}. When supplied, the greedy ordering is computed on the filtered
node set.}

\item{individuals}{Optional individual mapping used when \code{x} is a data frame
or file path rather than a prebuilt \code{\link{SynMultiAlignment}}.}

\item{odgi}{Optional path to the \code{odgi} executable. Used when \code{x} is a raw
\code{.og} graph path.}

\item{python}{Optional path to the Python interpreter. Used when \code{x} is a raw
\code{.og} graph path.}
}
\value{
A character vector of species identifiers in greedy comparison order.
}
\description{
Builds a comparison-chain order for an ODGI multiple alignment by starting at
one reference species and repeatedly choosing the remaining species that
shares the most ODGI nodes with the most recently chosen species. This is the
same heuristic used by \code{geom_nuclink(reference = ...)} to arrange ODGI-backed
comparison panels when multiple species are plotted together.
}
\examples{
tbl <- data.frame(
  node_id = 1:3,
  sequence = c("A", "C", "G"),
  XZ1516_chromosome = "V_RagTag",
  XZ1516_strand = c("+", "+", "+"),
  XZ1516_absolute_start = c(100L, 101L, 102L),
  XZ1516_absolute_end = c(100L, 101L, 102L),
  N2_chromosome = "V",
  N2_strand = c("+", "+", "NA"),
  N2_absolute_start = c(200L, 201L, "NA"),
  N2_absolute_end = c(200L, 201L, "NA"),
  CB4856_chromosome = "V",
  CB4856_strand = c("+", "NA", "NA"),
  CB4856_absolute_start = c(300L, "NA", "NA"),
  CB4856_absolute_end = c(300L, "NA", "NA"),
  check.names = FALSE,
  stringsAsFactors = FALSE
)

odgi_species_order(tbl, reference_species = "XZ1516")
}
