% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-nuclink.R
\name{geom_synteny_link}
\alias{geom_synteny_link}
\title{Draw synteny interval links}
\usage{
geom_synteny_link(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  alignment = NULL,
  reference = NULL,
  chr = NULL,
  subset = NULL,
  filter_by_len = NULL,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
\code{colour}, \code{fill}, and \code{alpha} can be mapped in the standard ggplot2 way.}

\item{data}{A data frame or a \code{SynSpecies} object.}

\item{stat, position}{Standard ggplot2 layer arguments.}

\item{...}{Additional parameters passed to the layer, including fixed
aesthetics such as \code{fill}, \code{colour}, and \code{alpha}.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a
warning. If \code{TRUE}, missing values are removed silently.}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{alignment}{Optional alignment name when \code{data} is a \code{SynSpecies}.
This can refer to a stored \code{SynPairAlignment} or an ODGI
\code{SynMultiAlignment}.}

\item{reference}{Optional reference species used when deriving a comparative
window from \code{chr} + \code{subset}. For ODGI multiple alignments, this also seeds
the greedy comparison-panel order: starting from \code{reference}, ggexon
repeatedly picks the remaining species that shares the most ODGI nodes with
the most recently placed species.}

\item{chr}{Optional reference chromosome / seqname when subsetting Syn-backed
links.}

\item{subset}{Optional numeric length-2 reference window. When supplied
together with \code{reference} and \code{chr}, only links overlapping the derived
comparative region are drawn. When omitted, \code{geom_nuclink()} uses the
current annotation windows when available, otherwise the full alignment.}

\item{filter_by_len}{Optional ODGI node-length filter such as \code{"> 10"},
\code{"= 3"}, or \code{"<= 2"}. Applied only when link rows are being derived from an
ODGI multiple alignment.}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics rather than
combining with them.}
}
\value{
A ggplot2 layer using \code{GeomNucLink}.
}
\description{
\code{geom_synteny_link()} draws filled interval ribbons between genomic tracks
when the rows represent syntenic blocks, orthologous genes, conserved exons,
or other biologically matched intervals. It is a semantic wrapper around
\code{\link[=geom_nuclink]{geom_nuclink()}} for cases where the link is not necessarily a nucleotide
alignment fragment.
}
\details{
Use this layer when the input table already describes interval-to-interval
relationships. For ordinary data frames the expected columns are the same as
\code{\link[=geom_nuclink]{geom_nuclink()}}:
\itemize{
\item \code{tspecies}, \code{tchr}, \code{tstart}, \code{tend}
\item \code{qspecies}, \code{qchr}, \code{qstart}, \code{qend}
\item \code{strand}
}

For \code{SynSpecies} input, \code{alignment}, \code{reference}, \code{chr}, and \code{subset} are
forwarded to \code{\link[=geom_nuclink]{geom_nuclink()}} so stored pairwise or multiple alignments can
still be resolved lazily.

Internally, this wrapper creates the same \code{LayerSyn} and \code{GeomNucLink} layer
as \code{\link[=geom_nuclink]{geom_nuclink()}}. During plot build, \code{\link[=facet_genomics]{facet_genomics()}} creates annotation
panels and middle link panels, then attaches \code{target_anchor_y},
\code{query_anchor_y}, \code{t_panel}, and \code{q_panel} metadata. \code{GeomNucLink} then melts
\code{tstart}/\code{tend}/\code{qstart}/\code{qend} into four polygon vertices, maps target and
query x coordinates through their source annotation panels, and draws the
filled polygon in the link panel.
}
\seealso{
\code{\link[=geom_nuclink]{geom_nuclink()}}, \code{\link[=facet_genomics]{facet_genomics()}}, \code{\link[=geom_genetag]{geom_genetag()}}
}
