% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{create_layout2}
\alias{create_layout2}
\title{Create the ggexon layout runtime}
\usage{
create_layout2(facet, coord, layout = NULL)
}
\arguments{
\item{facet}{A facet ggproto object, usually \code{FacetGenomics} or a standard
ggplot2 facet.}

\item{coord}{A coordinate ggproto object.}

\item{layout}{Optional layout ggproto subclass. Defaults to \code{Layout2}.}
}
\value{
A ggproto layout object used internally by \code{ggexon_build()}.
}
\description{
\code{create_layout2()} builds the \code{Layout2} ggproto object used by
\code{ggexon_build()}. This runtime object sits between layers, facets, and
coordinates during plot build and is responsible for turning the facet's
panel table into panel-aware layer data.
}
\details{
In ggexon, \code{Layout2} extends the standard ggplot2 layout pipeline with Syn-
specific behavior:
\itemize{
\item \code{SynSpecies} / \code{SynIndividual} plot data can be carried into facet setup
without being treated as an ordinary data frame.
\item a stored \code{SynLayout} can be supplied as an override and reused during build.
\item link layers can trigger genomic panel reordering and panel metadata such as
\code{t_panel} / \code{q_panel}.
\item panel metadata is joined back onto layer data so geoms such as
\code{geom_nuclink()} can transform each side of a link against the correct
annotation panel.
}
}
\keyword{internal}
