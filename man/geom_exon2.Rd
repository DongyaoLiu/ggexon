% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-exon.R
\name{geom_exon2}
\alias{geom_exon2}
\title{Draw WormWeb-style exon-intron schematics}
\usage{
geom_exon2(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  transcripts_track_ratio = NULL,
  exon_height = NULL,
  x_translation = NULL,
  subset = NULL,
  annotation_type = "exon",
  species = NULL,
  chr = NULL,
  breakdata = NULL,
  compress_introns = TRUE,
  intron_width = NULL,
  intron_shape = c("chevron", "flat"),
  chevron_direction = c("up", "down"),
  utr_height = 0.45,
  cds_height = 1,
  intron_peak = 0.35,
  arrow_width = NULL,
  transcript_backbone_fill = NULL,
  transcript_backbone_colour = NULL,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{A data frame, \code{SynSpecies}, or \code{SynIndividual} object.}

\item{stat, position}{Standard ggplot2 layer arguments.}

\item{...}{Additional parameters passed on to the layer.}

\item{na.rm}{If \code{FALSE}, missing values are removed with a warning.}

\item{show.legend}{Logical. Should this layer be included in the legend?}

\item{transcripts_track_ratio}{Optional transcript track ratio used by the
ggexon layout helpers.}

\item{exon_height}{Optional maximum exon rectangle height.}

\item{x_translation}{Optional x offset applied before drawing.}

\item{subset}{Optional numeric length-2 genomic window to keep.}

\item{annotation_type}{Feature type to keep. The default \code{"exon"} matches
\code{\link[=geom_exon]{geom_exon()}}. Use \code{"all"} to keep exon, CDS, and UTR-like rows so UTR/CDS
heights can differ.}

\item{species}{Optional species / individual identifier when \code{data} is a
\code{SynSpecies}.}

\item{chr}{Optional chromosome / seqname restriction when \code{data} is Syn-backed.}

\item{breakdata}{Optional break specification passed to \code{addbreak()}.}

\item{compress_introns}{Logical. If \code{TRUE}, gaps between neighbouring feature
blocks are replaced by schematic intron gaps.}

\item{intron_width}{Width used for compressed introns. When \code{NULL}, ggexon
derives a width from the median feature width.}

\item{intron_shape}{\code{"chevron"} for angled WormWeb-like connectors or
\code{"flat"} for straight intron lines.}

\item{chevron_direction}{Direction for chevron introns, either \code{"up"} or
\code{"down"}.}

\item{utr_height}{Relative height for UTR-like rows.}

\item{cds_height}{Relative height for CDS rows. Exon rows also use this
height when no explicit UTR/CDS distinction is present.}

\item{intron_peak}{Relative height of the chevron peak.}

\item{arrow_width}{Width of the terminal strand-direction triangle. When
\code{NULL}, ggexon derives a width from the terminal exon block.}

\item{transcript_backbone_fill, transcript_backbone_colour}{Optional fixed
fill and line/outline colour for intron connectors and terminal direction
arrows. When \code{NULL}, ggexon preserves the existing inherited/default
aesthetics for backwards compatibility.}

\item{inherit.aes}{If \code{FALSE}, overrides inherited aesthetics.}
}
\value{
A ggplot2 layer using \code{GeomExon2}.
}
\description{
\code{geom_exon2()} draws a publication-style transcript schematic inspired by
WormWeb exon/intron cartoons. Compared with \code{\link[=geom_exon]{geom_exon()}}, it draws introns
explicitly between neighbouring feature blocks, can compress long introns,
and uses thinner boxes for UTR features when UTR/CDS rows are available.
}
\details{
The layer keeps the same Syn-aware grammar as \code{\link[=geom_exon]{geom_exon()}}: it can consume a
data frame directly, or lazily resolve \code{SynIndividual} / \code{SynSpecies} inputs
during plot build.
}
