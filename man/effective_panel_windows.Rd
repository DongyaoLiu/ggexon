% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-windows.R
\name{effective_panel_windows}
\alias{effective_panel_windows}
\title{Inspect effective annotation panel windows}
\usage{
effective_panel_windows(x)
}
\arguments{
\item{x}{A ggexon plot object, \code{\link{SynSpecies}}, or \code{\link{SynLayout}} object.}
}
\value{
A \code{data.frame} with one row per annotation panel.
}
\description{
Returns a compact table describing the annotation-panel windows currently in
effect. For a ggexon plot object, this inspects the built plot so the result
reflects the actual panel selection and x ranges after stored layouts,
\code{set_panel_xlim()}, explicit \verb{subset =}, and link-derived windows have all
been resolved.
}
\details{
The returned columns distinguish between requested layout windows and the
ranges actually observed in built annotation data:
\itemize{
\item \code{chr}, \code{start}, \code{end}: window stored on the layout, usually from
\code{\link[=set_panel_xlim]{set_panel_xlim()}} or direct \code{SynLayout} edits.
\item \code{observed_start}, \code{observed_end}: min/max \code{xmin} and \code{xmax} seen in the
built annotation layers for that panel.
}

These values are often different. For example, if you request
\verb{start = 0, end = 5000} on a scaffold but the first annotated exon starts at
1283 and the last one ends at 1871, then the observed range will be
\verb{1283..1871} even though the effective panel window remains \verb{0..5000}.
This helper is therefore useful both for checking stored panel windows and
for seeing how much of that window is actually occupied by annotation.
}
