% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SynIndividual-class.R
\name{subset_feature_annotation}
\alias{subset_feature_annotation}
\title{Subset a feature annotation layer by genomic window}
\usage{
subset_feature_annotation(
  x,
  annotation = NULL,
  individual = NULL,
  chr = NULL,
  start = NULL,
  end = NULL,
  coords = NULL,
  gene = NULL,
  transcript = NULL
)
}
\arguments{
\item{x}{A \code{SynFeatureAnnotation}, \code{SynIndividual}, or \code{SynSpecies} object.}

\item{annotation}{Optional feature-annotation layer name when \code{x} is a
\code{SynIndividual} or \code{SynSpecies}. Defaults to the active feature annotation.}

\item{individual}{Optional individual name when \code{x} is a \code{SynSpecies}.}

\item{chr}{Chromosome or sequence name to keep.}

\item{start, end}{Optional numeric window bounds. If one bound is omitted,
the helper expands to the start or end of the selected sequence present in
the annotation.}

\item{coords}{Optional coordinate string in the form
\code{"V_RagTag:21559983-21620009"}. Use either \code{coords} or \code{chr}/\code{start}/\code{end}.}
}
\value{
Returns an object of the same top-level class supplied to \code{x}: a
\code{SynFeatureAnnotation}, updated \code{SynIndividual}, or updated \code{SynSpecies}.
}
\description{
Returns a windowed snapshot of a \code{\link{SynFeatureAnnotation}}. The current
active annotation and its base annotation are trimmed to the requested
genomic interval. Patch history and derived caches are cleared so the
returned object behaves as a self-contained view of the selected region.
}
\details{
This is an S4 generic that dispatches on the class of \code{x}.
}
\examples{
ann_path <- system.file(
  "extdata",
  "caenorhabditis_XZ1516.gff3",
  package = "ggexon"
)
ind <- SynIndividual(
  annotation_file = ann_path,
  genome_file = genome_waiver(),
  id = "XZ1516"
) |>
  load_annotation()

gr <- annotation_data(ind)
chr <- as.character(GenomeInfoDb::seqnames(gr))[[1L]]
start <- IRanges::start(gr)[[1L]]
end <- IRanges::end(gr)[[1L]]

ann_view <- subset_feature_annotation(
  get_annotation(ind, "default"),
  chr = chr,
  start = start,
  end = end
)
ind_view <- subset_feature_annotation(ind, chr = chr, start = start, end = end)

}
