% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-protein-lollipop.R
\name{protein_lollipop_data}
\alias{protein_lollipop_data}
\title{Prepare lollipop annotation data on a linear feature track}
\usage{
protein_lollipop_data(
  mutations,
  domains = NULL,
  protein_length = NULL,
  position = "position",
  label = NULL,
  ref = NULL,
  alt = NULL,
  score = NULL,
  domain_start = "start",
  domain_end = "end",
  domain = NULL,
  domain_ymin = NULL,
  domain_ymax = NULL,
  spread_threshold = 7,
  protein_start = 0,
  track_ymin = 0.4,
  track_ymax = 0.6,
  domain_default_ymin = 0.35,
  domain_default_ymax = 0.65,
  mutation_y = 1,
  mutation_y_by = NULL,
  mutation_y_strategy = c("scaled", "bins"),
  mutation_y_range = c(0.85, 1.45),
  mutation_y_trans = "identity",
  mutation_y_breaks = NULL,
  mutation_y_values = NULL,
  stem_points = 30,
  curve_k = 1
)
}
\arguments{
\item{mutations}{A data frame with one row per mutation.}

\item{domains}{Optional interval table, \code{S4Vectors::DataFrame}, or
\code{SynProteinDomainAnnotation} object. Intervals must use the same coordinate
system as \code{mutations}.}

\item{protein_length}{Optional track length. When \code{NULL}, the maximum
mutation position/domain end is used.}

\item{position}{Column in \code{mutations} containing mutation positions.}

\item{label}{Optional label column in \code{mutations}. When omitted, a
\code{mutation} column is used if present; otherwise labels are built from
\code{ref}, \code{position}, and \code{alt} when both amino-acid columns are supplied.}

\item{ref, alt}{Optional reference and alternate residue/base columns used to
generate labels.}

\item{score}{Optional column in \code{mutations} used for lollipop point colour,
for example a BLOSUM score.}

\item{domain_start, domain_end}{Domain interval start/end columns.}

\item{domain}{Optional domain label column. When omitted, a suitable domain
column is inferred from common InterProScan/domain-table names.}

\item{domain_ymin, domain_ymax}{Optional domain y-bound columns. If omitted,
existing \code{ymin}/\code{ymax} columns are used when present.}

\item{spread_threshold}{Minimum x-distance between adjacent lollipop heads.}

\item{protein_start}{Start coordinate for the backbone rectangle.}

\item{track_ymin, track_ymax}{Backbone y-range.}

\item{domain_default_ymin, domain_default_ymax}{Default y-range for domain
intervals when the interval table does not already contain y columns.}

\item{mutation_y}{Fixed y coordinate for lollipop heads when
\code{mutation_y_by} is \code{NULL}.}

\item{mutation_y_by}{Optional numeric column in \code{mutations} used to map
lollipop heads to multiple y levels, for example \code{"sample_count"}.}

\item{mutation_y_strategy}{Height-mapping strategy used when
\code{mutation_y_by} is supplied. \code{"scaled"} rescales values into
\code{mutation_y_range}; \code{"bins"} assigns values to tiered y levels.}

\item{mutation_y_range}{Numeric length-2 y range used by non-fixed height
strategies.}

\item{mutation_y_trans}{Transformation applied before height mapping. One
of \code{"identity"}, \code{"log10"}, \code{"log2"}, \code{"log"}, \code{"sqrt"}, or a function.
Log and square-root transformations require non-negative values and logs
use \code{value + 1}, which is convenient for count data.}

\item{mutation_y_breaks}{Optional numeric upper bounds for
\code{mutation_y_strategy = "bins"}, expressed on the original
\code{mutation_y_by} scale.}

\item{mutation_y_values}{Optional numeric y levels for
\code{mutation_y_strategy = "bins"}. When \code{mutation_y_breaks} is supplied,
this must have length \code{length(mutation_y_breaks) + 1}.}

\item{stem_points}{Number of points used per stem curve.}

\item{curve_k}{Sigmoid steepness for curved stems.}
}
\value{
A list with \code{mutations}, \code{domains}, \code{backbone}, and \code{stems} data
frames.
}
\description{
\code{protein_lollipop_data()} prepares the small data frames needed to draw a
mutation lollipop plot. Coordinates are treated as positions on one linear
track, so the same structure can be used for amino-acid positions, CDS
positions, or nucleotide positions after upstream coordinate conversion.
}
