% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/facet-genomics.R
\name{facet_genomics}
\alias{facet_genomics}
\title{Facet genomic tracks and link panels}
\usage{
facet_genomics(
  facets,
  nrow = NULL,
  ncol = NULL,
  scales = "fixed",
  shrink = TRUE,
  labeller = "label_value",
  as.table = TRUE,
  switch = deprecated(),
  drop = TRUE,
  dir = "h",
  strip.position = "top",
  axes = "margins",
  axis.labels = "all"
)
}
\arguments{
\item{facets}{Faceting variables, usually \code{ggplot2::vars(track)} for Syn
layouts.}

\item{nrow, ncol}{Number of rows and columns in the wrapped layout.}

\item{scales}{One of \code{"fixed"}, \code{"free_x"}, \code{"free_y"}, or \code{"free"}.}

\item{shrink}{Passed through to the facet.}

\item{labeller}{A labeller specification.}

\item{as.table}{Logical; whether panels are laid out like a table.}

\item{switch}{Deprecated ggplot2 argument.}

\item{drop}{Logical; drop unused facet levels?}

\item{dir}{Wrapping direction. Single-letter values are normalized using
\code{as.table}.}

\item{strip.position}{Position of facet strips.}

\item{axes}{Which axes to draw.}

\item{axis.labels}{Which axis labels to draw.}
}
\value{
A \code{FacetGenomics} ggproto object.
}
\description{
\code{facet_genomics()} is ggexon's Syn-aware faceting interface. It behaves like
a wrap-style facet for ordinary data, but it can also arrange comparative
genomic panels for \code{SynSpecies} plots, including annotation panels and
intermediate link panels used by \code{geom_nuclink()}.
}
\details{
For \code{SynSpecies} inputs, the facet chooses among three layout sources:
\itemize{
\item an explicit layout override attached during build
\item a stored \code{\link{SynLayout}} on the \code{SynSpecies} object
\item a newly derived chain layout computed from the annotation and link layers
}

When link panels are present, \code{facet_genomics()} also annotates the final
panel table with source-panel metadata (\code{t_panel}, \code{q_panel}) and vertical
link anchors so that \code{geom_nuclink()} can borrow x ranges from the correct
annotation panels while drawing inside the link panel.
}
\section{SynSpecies behavior}{

In Syn-aware builds, \code{facet_genomics()} is responsible for deciding the panel
structure used by \code{Layout2}. The returned panel table may include:
\itemize{
\item annotation panels for each species track
\item link panels inserted between paired species tracks
\item \code{panel_type}, \code{tspecies}, \code{qspecies}, \code{t_panel}, and \code{q_panel} metadata
used later by \code{geom_nuclink()}
}

If no Syn-specific layout is available, the facet falls back to ordinary
wrap-style panel generation.
}

\seealso{
\link{SynLayout}
}
