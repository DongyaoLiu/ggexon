% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\docType{data}
\name{Layout2}
\alias{Layout2}
\title{ggexon layout runtime with Syn-aware panel setup}
\format{
An object of class \code{Layout2} (inherits from \code{Layout}, \code{ggproto}, \code{gg}) of length 3.
}
\usage{
Layout2
}
\description{
\code{Layout2} is ggexon's custom layout ggproto. It inherits from ggplot2's
\code{Layout} and overrides the parts of the build pipeline where Syn-aware panel
structure and link metadata need to be introduced.
}
\details{
Compared with the upstream layout, \code{Layout2} adds two main responsibilities:
\enumerate{
\item \code{setup()} carries Syn plot data and optional stored \code{SynLayout} metadata
into facet setup, lets \code{facet_genomics()} generate or reuse genomic
panel layouts, and joins panel-level metadata such as \code{t_panel} and
\code{q_panel} back onto layer data.
\item \code{map_position()} maps x/y aesthetics panel-by-panel using the trained
scales from the resolved layout while preserving the extra panel metadata
introduced during setup.
}

This class is what makes stored \code{SynLayout} objects, link panels, and
cross-panel coordinate borrowing work inside the normal ggplot2 build flow.
}
\section{Build flow}{

The high-level flow is:
\itemize{
\item \code{ggexon_build()} creates \code{Layout2} with \code{create_layout2()}.
\item \code{Layout2$setup()} asks the active facet for the panel table.
\item \code{facet_genomics()} may return a stored \code{SynLayout}, derive a new
chain layout, or fall back to a standard faceting layout.
\item if link panels are present, link-direction metadata is added and source
panel ids are propagated to layer data.
\item \code{Layout2$map_position()} maps each layer's x/y aesthetics against the
\code{SCALE_X} / \code{SCALE_Y} assignments in the final panel table.
}
}

\seealso{
\link{SynLayout}
}
\keyword{datasets}
