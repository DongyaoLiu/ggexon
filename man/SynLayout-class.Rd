% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SynSpecies-class.R
\docType{class}
\name{SynLayout-class}
\alias{SynLayout-class}
\title{SynLayout class}
\description{
\code{SynLayout} stores panel placement information used by \code{facet_genomics()}
together with shared plotting defaults that can be resolved by syn-aware
geoms. The \code{panels} table describes the panel arrangement, while the numeric
slots store layout-scoped defaults such as shared exon height or x-axis
translation.
}
\section{Slots}{

\describe{
\item{\code{panels}}{Layout data frame. It must contain \code{PANEL}, \code{ROW}, \code{COL}, and
\code{track}, and may also contain comparative plotting columns such as
\code{panel_type}, \code{species}, \code{alignment_name}, \code{tspecies}, \code{qspecies},
\code{t_panel}, \code{q_panel}, \code{x_source_panel}, \code{SCALE_X}, \code{SCALE_Y}, and optional
panel-specific x-window columns \code{xlim_chr}, \code{xlim_min}, and \code{xlim_max}.}

\item{\code{layout_type}}{Scalar layout strategy label such as \code{"custom"} or
\code{"chain"}.}

\item{\code{free}}{List with logical \code{x} and \code{y} entries describing whether scales
should vary across panels.}

\item{\code{exon_height}}{Shared default exon or gene block height for
layout-aware annotation geoms.}

\item{\code{x_translation}}{Shared default x-axis offset for layout-aware annotation
geoms.}

\item{\code{metadata}}{Optional layout metadata.}
}}

\section{Prototype defaults}{

\itemize{
\item \code{panels = data.frame()}
\item \code{layout_type = "custom"}
\item \code{free = list(x = FALSE, y = FALSE)}
\item \code{exon_height = NA_real_}
\item \code{x_translation = NA_real_}
\item \code{metadata = list()}
}
}

\section{Panel roles and inherited scales}{

Syn-aware layouts use explicit \code{panel_type} values such as \code{"annotation"},
\code{"coverage"}, and \code{"link"}. The same public \code{track} may therefore occur in
more than one role. \code{SCALE_Y} is the authoritative inherited scale-object
identity: panels with equal values share training, while panels with
different values train independently. Resolved role policies may be kept in
\code{metadata$panel_role_y_policies} so older or serialized layouts preserve
their fixed/free interpretation.
}

\section{Validity rules}{

\itemize{
\item \code{panels} must contain at least the columns \code{PANEL}, \code{ROW}, \code{COL}, and
\code{track}.
\item when \code{panels} contains any of \code{xlim_chr}, \code{xlim_min}, or \code{xlim_max}, it
must contain all three columns.
\item annotation panels with panel-specific x limits must provide complete
\code{xlim_min} and \code{xlim_max} values. \code{xlim_chr} may be missing for numeric
display-only windows that should not drive annotation filtering.
\item when multiple annotation panels define different x windows,
\code{free$x} must be \code{TRUE}.
\item \code{layout_type} must be one non-empty character value.
\item \code{free} must be a list with scalar logical \code{x} and \code{y} entries.
\item \code{exon_height} and \code{x_translation} must each be scalar numeric values.
}
}

