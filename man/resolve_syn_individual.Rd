% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/syn-layer.R
\name{resolve_syn_individual}
\alias{resolve_syn_individual}
\title{Resolve one individual from Syn-backed plot input}
\usage{
resolve_syn_individual(x, species = NULL)
}
\arguments{
\item{x}{A \code{\link{SynSpecies}} or \code{\link{SynIndividual}} object.}

\item{species}{Optional individual identifier. When \code{x} is a \code{SynSpecies}
with more than one stored individual, this argument is required.}
}
\value{
A single \code{\link{SynIndividual}} object.
}
\description{
Normalizes Syn plotting inputs so downstream layer helpers can work with a
single \code{\link{SynIndividual}} object. When \code{x} is already a \code{SynIndividual}, the
function returns it unchanged after optionally checking that \code{species}
matches its identifier. When \code{x} is a \code{\link{SynSpecies}} collection, the helper
selects one stored individual by name.
}
\details{
This function is mainly used inside Syn-aware geoms and query helpers that
allow users to supply either a whole \code{SynSpecies} object or an already
selected \code{SynIndividual}.

The helper throws an error when:
\itemize{
\item \code{x} is neither a \code{SynSpecies} nor a \code{SynIndividual}
\item the supplied \code{SynSpecies} has no individuals
\item \code{species} is omitted for a \code{SynSpecies} that stores multiple individuals
\item \code{species} does not match any stored individual
\item \code{species} is supplied for a \code{SynIndividual} but does not match
}
}
\keyword{internal}
