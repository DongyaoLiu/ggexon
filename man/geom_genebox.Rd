% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-genebox.R
\name{geom_genebox}
\alias{geom_genebox}
\title{Draw fixed-size gene boxes with internal strand arrows}
\usage{
geom_genebox(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  box_size = 3,
  anchor = c("middle", "start", "end"),
  arrow_colour = NULL,
  arrow_linewidth = 0.45,
  arrow_head_size = 0.7,
  species = NULL,
  chr = NULL,
  subset = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)
}
\arguments{
\item{mapping, data, stat, position, ..., na.rm, show.legend, inherit.aes}{Standard
ggplot2 layer arguments. When column names are literally \code{x}, \code{y}, and
\code{strand}, they are mapped automatically.}

\item{box_size}{Side length of each square in millimetres. Defaults to \code{3}.}

\item{anchor}{Genomic point derived for Syn-backed data: the annotated
initiation-codon centre or 5-prime terminal-CDS proxy (\code{"start"}), midpoint
between the selected start and end anchors (\code{"middle"}), or annotated
stop-codon centre or 3-prime terminal-CDS proxy (\code{"end"}). Defaults to
\code{"middle"}.}

\item{arrow_colour}{Optional fixed colour for the internal arrow. \code{NULL}
chooses black or white separately for each box to contrast with its fill.}

\item{arrow_linewidth}{Width of the internal arrow in millimetres.}

\item{arrow_head_size}{Length of the closed triangular arrow head in
millimetres.}

\item{species}{Optional individual identifier for \code{SynSpecies} input.}

\item{chr}{Optional chromosome or seqname restriction for Syn-backed data.}

\item{subset}{Optional numeric length-two genomic window for Syn-backed data.}
}
\value{
A ggplot layer.
}
\description{
\code{geom_genebox()} represents each gene with a square of fixed physical size
and a horizontal arrow indicating transcription direction. Because the box
dimensions are measured in millimetres, the symbol stays square under free
genomic scales and coordinate transformations.
}
\details{
For ordinary data frames, map \code{x}, \code{y}, and \code{strand}. In a ggexon plot backed
by a \link{SynIndividual} or \link{SynSpecies} object, the layer selects, for each gene,
the protein-coding transcript with the greatest genomic span. \code{anchor = "start"}
uses the middle nucleotide of a complete annotated initiation codon,
\code{anchor = "end"} uses the middle nucleotide of a complete annotated stop
codon, and \code{anchor = "middle"} uses the genomic midpoint between those two
anchors. Associated \code{start_codon} or \code{stop_codon} records are complete only
when their total feature width is exactly three nucleotides. Otherwise, the
middle position of the corresponding terminal CDS triplet is used as a
positional proxy; this fallback does not verify an ATG or stop-codon sequence.

Unknown strands (\code{NA}, \code{"*"}, or values other than \code{"+"} and \code{"-"}) retain
their square but omit the internal arrow. Reversing the x scale or x
coordinate direction reverses the visible arrow direction as well.
Supplying a \code{slot} column allows \code{\link[=strip_scale_x]{strip_scale_x()}} with \code{slot_order} to align
curated comparison slots exactly. Slot membership is supplied metadata, not
an inference of one-to-one homology or evolutionary loss. When a track has
enough ordered genes to infer whether the synthetic template reverses its
raw genomic direction, the internal arrows are corrected automatically;
\code{\link[=strip_scale_x]{strip_scale_x()}} warns and keeps the raw direction when this is
underdetermined.

Syn-derived data retain \code{genomic_x}, \code{anchor_start}, \code{anchor_middle},
\code{anchor_end}, \code{anchor_mode}, \code{transcript_id}, transcript-span columns, coding
bounds, gene identifiers, and (for \code{SynSpecies}) injected homology metadata.
The per-end \code{initiation_anchor_source} and \code{stop_anchor_source} columns record
either an explicit codon feature or \code{"terminal_CDS_positional_proxy"};
\code{initiation_anchor_fallback}, \code{stop_anchor_fallback}, and
\code{any_anchor_fallback} flag proxy use. These columns make transcript selection,
anchor interpretation, and downstream alignment auditable.
}
