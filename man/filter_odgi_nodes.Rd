% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odgi.R
\name{filter_odgi_nodes}
\alias{filter_odgi_nodes}
\title{Filter ODGI nodes by sequence length}
\usage{
filter_odgi_nodes(
  x,
  filter_by_len,
  alignment = NULL,
  odgi = NULL,
  python = NULL
)
}
\arguments{
\item{x}{A \code{\link{SynMultiAlignment}} with \code{format = "odgi"} or a \code{\link{SynSpecies}}
object containing one or more ODGI multiple alignments.}

\item{filter_by_len}{A single comparison string such as \code{"> 10"}, \code{"= 3"},
\code{"< 5"}, \code{">= 8"}, or \code{"<= 2"}.}

\item{alignment}{Optional multiple-alignment name when \code{x} is a
\code{\link{SynSpecies}}. If omitted and exactly one multiple alignment is stored,
that alignment is used.}

\item{odgi}{Optional path to the \code{odgi} executable. Used when an ODGI
alignment is backed by a raw \code{.og} graph and must be loaded first.}

\item{python}{Optional path to the Python interpreter. Used when an ODGI
alignment is backed by a raw \code{.og} graph and must be loaded first.}
}
\value{
An updated object of the same class as \code{x}.
}
\description{
Loads an ODGI-backed \code{\link{SynMultiAlignment}} if needed, then keeps only nodes
whose sequence length satisfies a comparison such as \code{"> 10"} or "\verb{<= 3}".
The returned object keeps the same alignment metadata and ODGI label mapping
but caches the filtered node table in memory.
}
\details{
When called on a \code{\link{SynSpecies}} object, \code{alignment} selects which stored
multiple alignment to update. The returned \code{SynSpecies} contains the filtered
\code{SynMultiAlignment} in place.
}
\examples{
tbl <- data.frame(
  node_id = 1:2,
  sequence = c("AC", "G"),
  XZ1516_chromosome = c("V_RagTag", "V_RagTag"),
  XZ1516_strand = c("+", "-"),
  XZ1516_absolute_start = c(100L, 102L),
  XZ1516_absolute_end = c(101L, 102L),
  N2_chromosome = c("V", "V"),
  N2_strand = c("+", "+"),
  N2_absolute_start = c(200L, 202L),
  N2_absolute_end = c(201L, 202L),
  check.names = FALSE,
  stringsAsFactors = FALSE
)

msa <- odgi_multi_alignment(tbl, name = "worm-graph")
filter_odgi_nodes(msa, "> 1")
}
