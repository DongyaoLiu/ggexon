% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-mutation-label.R
\name{geom_mutation_label}
\alias{geom_mutation_label}
\title{Draw mutation labels as a dedicated text layer}
\usage{
geom_mutation_label(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  mutations = NULL,
  annotation = NULL,
  individual = NULL,
  species = NULL,
  genes = NULL,
  event_type = NULL,
  min_sample_count = NULL,
  strains = NULL,
  mutation = NULL,
  mutation_position = "position",
  label = "mutation",
  ref = NULL,
  alt = NULL,
  spread_threshold = 7,
  mutation_y = 1,
  mutation_y_by = NULL,
  mutation_y_strategy = c("scaled", "bins"),
  mutation_y_range = c(0.85, 1.45),
  mutation_y_trans = "identity",
  mutation_y_breaks = NULL,
  mutation_y_values = NULL,
  label_nudge_y = 0.35,
  show_empty = FALSE,
  na.rm = FALSE,
  show.legend = FALSE,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, stat, position, ..., na.rm, show.legend, inherit.aes}{Standard
ggplot2 layer arguments.}

\item{mutations}{Optional mutation table. Used as \code{data} when \code{data} is
\code{NULL}.}

\item{annotation}{Optional mutation annotation-layer name.}

\item{individual}{Optional individual selector for \code{SynSpecies} input.}

\item{species}{Alias for \code{individual}, matching other Syn-aware geoms.}

\item{genes, event_type, min_sample_count, strains, mutation}{Optional filters
passed to \code{query_protein_mutations()}.}

\item{mutation_position}{Column containing mutation coordinates.}

\item{label}{Column used for text labels. Defaults to \code{"mutation"}.}

\item{ref, alt}{Optional residue/base columns used to build labels when
\code{label} is \code{NULL}.}

\item{spread_threshold}{Minimum x-distance between adjacent labels.}

\item{mutation_y}{Fixed y coordinate of mutation heads when
\code{mutation_y_by} is \code{NULL}.}

\item{mutation_y_by}{Optional numeric column used to align labels to
multi-height lollipop heads.}

\item{mutation_y_strategy, mutation_y_range, mutation_y_trans, mutation_y_breaks, mutation_y_values}{Height-mapping controls shared with \code{protein_lollipop_data()}.}

\item{label_nudge_y}{Vertical offset above mutation heads.}

\item{show_empty}{Logical; keep empty/\code{NA} labels when \code{TRUE}.}
}
\value{
A ggplot layer.
}
\description{
\code{geom_mutation_label()} is the text companion to mutation lollipop layers.
With ordinary data frames it prepares label positions from mutation
positions. With \code{SynIndividual} or \code{SynSpecies} input it dispatches mutation
rows from attached \code{SynProteinMutationAnnotation} layers.
}
