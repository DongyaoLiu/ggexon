% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-protein-lollipop.R
\name{geom_protein_lollipop}
\alias{geom_protein_lollipop}
\title{Draw lollipop annotations on a protein-coordinate track}
\usage{
geom_protein_lollipop(
  mutations,
  domains = NULL,
  protein_length = NULL,
  position = "position",
  label = NULL,
  ref = NULL,
  alt = NULL,
  score = NULL,
  domain_start = "start",
  domain_end = "end",
  domain = NULL,
  domain_ymin = NULL,
  domain_ymax = NULL,
  spread_threshold = 7,
  protein_start = 0,
  track_ymin = 0.4,
  track_ymax = 0.6,
  domain_default_ymin = 0.35,
  domain_default_ymax = 0.65,
  mutation_y = 1,
  stem_points = 30,
  curve_k = 1,
  show_backbone = TRUE,
  show_domains = TRUE,
  show_stems = TRUE,
  show_points = TRUE,
  show_labels = TRUE,
  backbone_fill = "grey80",
  backbone_colour = NA,
  domain_colour = NA,
  stem_colour = "grey30",
  stem_linewidth = 0.4,
  point_size = 4,
  point_colour = "black",
  label_size = 3.5,
  label_nudge_y = 0.35,
  label_angle = 90,
  show.legend = NA
)
}
\arguments{
\item{mutations}{A data frame with one row per mutation.}

\item{domains}{Optional interval table, \code{S4Vectors::DataFrame}, or
\code{SynProteinDomainAnnotation} object. Intervals must use the same coordinate
system as \code{mutations}.}

\item{protein_length}{Optional track length. When \code{NULL}, the maximum
mutation position/domain end is used.}

\item{position}{Column in \code{mutations} containing mutation positions.}

\item{label}{Optional label column in \code{mutations}. When omitted, a
\code{mutation} column is used if present; otherwise labels are built from
\code{ref}, \code{position}, and \code{alt} when both amino-acid columns are supplied.}

\item{ref, alt}{Optional reference and alternate residue/base columns used to
generate labels.}

\item{score}{Optional column in \code{mutations} used for lollipop point colour,
for example a BLOSUM score.}

\item{domain_start, domain_end}{Domain interval start/end columns.}

\item{domain}{Optional domain label column. When omitted, a suitable domain
column is inferred from common InterProScan/domain-table names.}

\item{domain_ymin, domain_ymax}{Optional domain y-bound columns. If omitted,
existing \code{ymin}/\code{ymax} columns are used when present.}

\item{spread_threshold}{Minimum x-distance between adjacent lollipop heads.}

\item{protein_start}{Start coordinate for the backbone rectangle.}

\item{track_ymin, track_ymax}{Backbone y-range.}

\item{domain_default_ymin, domain_default_ymax}{Default y-range for domain
intervals when the interval table does not already contain y columns.}

\item{mutation_y}{Y coordinate for lollipop heads.}

\item{stem_points}{Number of points used per stem curve.}

\item{curve_k}{Sigmoid steepness for curved stems.}

\item{show_backbone, show_domains, show_stems, show_points, show_labels}{Logical
switches for layer components.}

\item{backbone_fill, backbone_colour}{Backbone rectangle styling.}

\item{domain_colour}{Domain rectangle outline colour.}

\item{stem_colour, stem_linewidth}{Stem styling.}

\item{point_size, point_colour}{Mutation point styling. \code{point_colour} is used
only when \code{score} is \code{NULL}; otherwise point colour is mapped to
\code{lollipop_score}.}

\item{label_size, label_nudge_y, label_angle}{Label text styling.}

\item{show.legend}{Passed to generated ggplot2 layers.}
}
\value{
A list of ggplot2 layers.
}
\description{
This helper returns a list of ggplot2 layers: backbone, optional domains,
curved stems, mutation heads, and labels. It is designed for protein tracks
but only assumes that mutations and domains share the same linear coordinate
system.
}
