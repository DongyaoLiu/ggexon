% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-genetag.R
\name{geom_genetag}
\alias{geom_genetag}
\title{Draw gene tags as exon bodies with strand-direction triangles}
\usage{
geom_genetag(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  exon_height = NULL,
  height = NULL,
  arrow_width = NULL,
  arrow_fraction = 0.18,
  species = NULL,
  chr = NULL,
  subset = NULL,
  feature_type = "gene",
  show_label = TRUE,
  label_position = NULL,
  label_direction = NULL,
  label_offset_fraction = NULL,
  label_link = NULL,
  label_link_type = NULL,
  collapse_tandem = NULL,
  check_overlap = FALSE,
  label_max_lanes = NULL,
  label_panel_width = NULL,
  label_genes = NULL,
  label_filter = NULL,
  label_match_by = NULL,
  label_match = NULL,
  label_size = NULL,
  label_colour = NULL,
  label_alpha = NULL,
  label_family = NULL,
  label_fontface = NULL,
  label_lineheight = NULL,
  label_link_colour = NULL,
  label_link_linewidth = NULL,
  label_link_linetype = NULL,
  label_link_alpha = NULL,
  panel_width_mm = NULL,
  panel_width_inch = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)
}
\arguments{
\item{mapping, data, stat, position, ..., na.rm, show.legend, inherit.aes}{Standard
ggplot2 layer arguments. \code{inherit.aes} defaults to \code{FALSE} so the layer can
be used cleanly in \code{ggtree::facet_plot()} side panels.}

\item{exon_height}{Total tag height in y-axis units. Defaults to \code{0.8}.}

\item{height}{Deprecated-compatible alias for \code{exon_height}.}

\item{arrow_width}{Optional width of the terminal triangle in x-axis units.
When \code{NULL}, width is calculated from \code{arrow_fraction}.}

\item{arrow_fraction}{Fraction of each gene span used for the terminal
triangle when \code{arrow_width} is \code{NULL}.}

\item{species}{Optional species / individual identifier when \code{data} is a
\code{SynSpecies}.}

\item{chr}{Optional chromosome / seqname restriction when \code{data} is
Syn-backed.}

\item{subset}{Optional numeric length-2 genomic window to keep.}

\item{feature_type}{Feature type passed to \code{\link[=query_features]{query_features()}}. Defaults to
\code{"gene"}.}

\item{show_label}{Logical; draw gene labels. Defaults to \code{TRUE}.}

\item{label_position}{Label placement mode. \code{"auto"} draws labels inside
tags when they fit and falls back outside otherwise; \code{"inside"} keeps the
previous inside-only behavior; \code{"outside"} draws all labels outside the
tag; \code{"none"} suppresses labels.}

\item{label_direction}{Outside label position. Accepts \code{"top"}, \code{"bottom"},
\code{"center"}, or colon-delimited combinations such as \code{"top:bottom"}.
Outside fallback treats \code{"center"} labels that do not fit as \code{"top"}.}

\item{label_offset_fraction}{Distance between the tag and outside label line,
as a fraction of \code{exon_height}.}

\item{label_link}{Logical; draw leader links for outside labels.}

\item{label_link_type}{Leader line style: \code{"straight"}, \code{"elbow"}, or
\code{"spline"}.}

\item{collapse_tandem}{When \code{TRUE}, consecutive outside labels with the same
displayed \code{label} in a track are collapsed into one label.}

\item{check_overlap}{Logical passed to text drawing for opt-in label overlap
suppression.}

\item{label_max_lanes}{Maximum number of vertical lanes available for
outside labels on each side of a track. Defaults to \code{3}.}

\item{label_panel_width}{Panel width used for label layout. The default
\code{"auto"} measures the final panel viewport at draw time. A positive
numeric value is interpreted as millimetres.}

\item{label_genes}{Optional gene selector for partial labeling. A character
vector applies globally; a named list applies per track.}

\item{label_filter}{Semantic label filter. Multiple values are OR-combined.
\code{"all"} preserves the default behavior; \code{"homology_hit"} and
\code{"species_specific"} use homology table hits; \code{"homology_anchor"},
\code{"homology_visible"}, and \code{"homology_offtrack"} require strip-scale
metadata.}

\item{label_match_by}{Columns used to match \code{label_genes}. \code{"auto"} checks
common gene and reference-gene identifier columns.}

\item{label_match}{Matching mode for \code{label_genes}: \code{"exact"} or \code{"regex"}.}

\item{label_size, label_colour, label_alpha, label_family, label_fontface, label_lineheight}{Fixed label styling used when \code{show_label = TRUE}. These can also be mapped
as aesthetics with names such as \code{aes(label_colour = ...)}.}

\item{label_link_colour, label_link_linewidth, label_link_linetype, label_link_alpha}{Fixed leader-link styling for outside labels. These can also be mapped as
aesthetics with the same names.}

\item{panel_width_mm, panel_width_inch}{Optional panel width for estimating
whether labels fit inside transformed gene tags.}
}
\value{
A ggplot layer.
}
\description{
\code{geom_genetag()} draws each stranded gene as a rectangular exon-like body
plus a symmetric terminal triangle. The triangle apex points toward the
strand-specific end of the feature. It is designed for gene-level summaries,
including ggtree side panels generated with \code{ggtree::facet_plot()}.
}
