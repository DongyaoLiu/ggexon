% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SynSpecies-class.R
\name{add_individuals_from_folder}
\alias{add_individuals_from_folder}
\title{Add many annotation files from a folder as SynIndividuals}
\usage{
add_individuals_from_folder(
  x,
  folder,
  annotation_format = c("auto", "gff", "gtf"),
  recursive = FALSE
)
}
\arguments{
\item{x}{A \code{\link{SynSpecies}} object.}

\item{folder}{Path to a directory containing annotation files.}

\item{annotation_format}{One of \code{"auto"}, \code{"gff"}, or \code{"gtf"}. When \code{"auto"},
files with supported extensions are discovered and each file's format is
inferred from its extension. When \code{"gff"} or \code{"gtf"}, only files with
matching extensions are imported.}

\item{recursive}{Logical; should files be discovered recursively?}
}
\value{
An updated \code{\link{SynSpecies}} object.
}
\description{
Discovers supported annotation files in a folder and adds one
\code{\link{SynIndividual}} per file to a \code{\link{SynSpecies}} object. Supported extensions
are \code{.gff}, \code{.gff3}, and \code{.gtf} (optionally with a trailing \code{.gz}). When
\code{annotation_format = "auto"}, the format is inferred from the file
extension.
}
\details{
Individual ids default to the filename stem with the annotation extension
removed, so a file such as \code{N2.gff3} becomes a \code{SynIndividual} with id
\code{"N2"}.

Genome files are waived by default for this convenience import. That makes
the helper suitable for annotation-only workflows, while genome-dependent
operations can still be added later by replacing or rebuilding the
\code{SynIndividual} objects with FASTA paths.
}
