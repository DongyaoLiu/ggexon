% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locus-set.R
\name{infer_locus_windows}
\alias{infer_locus_windows}
\title{Infer comparable locus windows from Syn annotations}
\usage{
infer_locus_windows(
  x,
  loci,
  anchors = NULL,
  name = "locus_windows",
  individual = NULL,
  reference = NULL,
  flank = 450000,
  annotation = NULL,
  feature_type = "gene",
  prefix_anchors = TRUE,
  missing = c("drop", "error")
)
}
\arguments{
\item{x}{A \code{SynSpecies} object.}

\item{loci}{Character vector of focal loci / grid columns.}

\item{anchors}{Optional character vector used for every locus, or a named
list of anchor genes keyed by \code{loci}.}

\item{name}{Locus-set name.}

\item{individual}{Optional individuals to include. Defaults to all.}

\item{reference}{Optional reference individual recorded in metadata.}

\item{flank}{Number of bases to add around direct focal genes and anchor
clusters.}

\item{annotation}{Optional feature annotation layer to query.}

\item{feature_type}{Feature type used for gene rows. Defaults to \code{"gene"}.}

\item{prefix_anchors}{Treat anchor names as gene-prefixes. Useful for gene
families such as \code{NBPF}.}

\item{missing}{What to do when neither a focal gene nor anchors are found.}
}
\value{
A \code{SynLocusSet} object.
}
\description{
Searches each individual in a \code{SynSpecies} for direct focal genes and, when
missing, falls back to anchor genes to infer a local syntenic window. This
helper returns a \code{SynLocusSet}; attach it with \code{\link[=add_locus_set]{add_locus_set()}} and call
\code{\link[=use_locus_grid]{use_locus_grid()}} to store the corresponding panel layout.
}
