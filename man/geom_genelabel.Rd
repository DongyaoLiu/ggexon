% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_genelabel.R
\name{geom_genelabel}
\alias{geom_genelabel}
\title{Draw gene labels on exon tracks}
\usage{
geom_genelabel(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  x_translation = NULL,
  ...,
  na.rm = FALSE,
  show.legend = NA,
  exon_height = NULL,
  label_direction = NULL,
  label_offset_fraction = NULL,
  species = NULL,
  chr = NULL,
  subset = NULL,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, stat, position, ..., na.rm, show.legend, inherit.aes}{Standard
ggplot2 layer arguments.}

\item{x_translation}{Optional x offset applied before drawing.}

\item{exon_height}{Optional exon rectangle height used when preparing track
coordinates.}

\item{label_direction}{Where to place the label line: \code{"top"} (above the
highest track), \code{"bottom"} (below the lowest track), or \code{"both"}
(odd-indexed labels above, even-indexed labels below). Default \code{"top"}.}

\item{label_offset_fraction}{Distance between the exon tracks and the label
line, expressed as a fraction of \code{exon_height}. Default \code{0.3}.}

\item{species}{Optional species / individual identifier when \code{data} is a
\code{SynSpecies}.}

\item{chr}{Optional chromosome / seqname restriction when \code{data} is
Syn-backed.}

\item{subset}{Optional numeric length-2 genomic window to keep.}
}
\value{
A ggplot2 layer using the internal \code{GeomGeneLabel} ggproto.
}
\description{
\code{geom_genelabel()} places one text label per transcript or gene span on an
exon-style genomic track. Labels sit on a single horizontal line above or
below the exon tracks, with leader lines connecting each label to its gene
body. Overlapping labels are pushed apart horizontally.
}
