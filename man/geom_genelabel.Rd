% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_genelabel.R
\name{geom_genelabel}
\alias{geom_genelabel}
\title{Draw gene labels on exon tracks}
\usage{
geom_genelabel(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  x_translation = NULL,
  ...,
  na.rm = FALSE,
  show.legend = NA,
  exon_height = NULL,
  label_direction = NULL,
  label_offset_fraction = NULL,
  link_type = NULL,
  collapse_tandem = NULL,
  show_link = NULL,
  homology_name = NULL,
  species = NULL,
  chr = NULL,
  subset = NULL,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, stat, position, ..., na.rm, show.legend, inherit.aes}{Standard
ggplot2 layer arguments.}

\item{x_translation}{Optional x offset applied before drawing.}

\item{exon_height}{Optional exon rectangle height used when preparing track
coordinates.}

\item{label_direction}{One or more label positions joined with \code{:}, e.g.
\code{"top"}, \code{"bottom"}, \code{"top:bottom"}, or \code{"bottom:top:center"}.
Each gene receives a position based on its track index modulo the number
of position tokens:
\itemize{
\item \code{"top"} — all labels above the highest track.
\item \code{"bottom"} — all labels below the lowest track.
\item \code{"top:bottom"} — odd-indexed genes above, even-indexed below.
\item \code{"bottom:top:center"} — gene 1 bottom, gene 2 top, gene 3 centre (and
repeats). Genes assigned \code{"center"} have their label placed on the
gene body. If the label text is wider than the gene span, the label
falls back to \code{"top"}.
}

Valid tokens: \code{"top"}, \code{"bottom"}, \code{"center"}. Default \code{"top"}.}

\item{label_offset_fraction}{Distance between the exon tracks and the label
line, expressed as a fraction of \code{exon_height}. Default \code{0.3}.}

\item{link_type}{Leader line style: \code{"straight"} (direct line),
\code{"elbow"} (right-angle bend via vertical then horizontal segment),
or \code{"spline"} (smooth Bézier curve). Default \code{"straight"}.
Centre-fitting labels do not draw leader lines.}

\item{collapse_tandem}{When \code{TRUE}, consecutive genes with identical labels
(tandem duplications) share a single label connected to all gene bodies
by a bracket-style connector. Default \code{FALSE}.}

\item{show_link}{When \code{TRUE} (the default), leader lines are drawn between
gene bodies and labels. Set to \code{FALSE} to suppress all leader lines
(only the text labels are rendered).}

\item{homology_name}{Optional name of a \code{HomologyAnnotation} stored on the
\code{SynSpecies} object. When set, gene labels are replaced with the
corresponding reference-species gene names using the homology mapping.
This is useful for labelling query-species genes with the names of their
orthologs in the best-annotated center species.}

\item{species}{Optional species / individual identifier when \code{data} is a
\code{SynSpecies}.}

\item{chr}{Optional chromosome / seqname restriction when \code{data} is
Syn-backed.}

\item{subset}{Optional numeric length-2 genomic window to keep.}
}
\value{
A ggplot2 layer using the internal \code{GeomGeneLabel} ggproto.
}
\description{
\code{geom_genelabel()} places one text label per transcript or gene span on an
exon-style genomic track. The \code{label_direction} parameter accepts
colon-delimited combinations to distribute labels across positions using
modulo assignment.
}
