% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SynSpecies-class.R
\name{add_tree}
\alias{add_tree}
\alias{add_tree,SynSpecies-method}
\alias{add_tree,ANY-method}
\title{Add a tree or tree plot to a \code{SynSpecies} object}
\usage{
add_tree(x, tree = NULL, tree_file = NULL, tree_plot = NULL, ...)

\S4method{add_tree}{SynSpecies}(x, tree = NULL, tree_file = NULL, tree_plot = NULL, ...)

\S4method{add_tree}{ANY}(x, tree = NULL, tree_file = NULL, tree_plot = NULL, ...)
}
\arguments{
\item{x}{A \code{SynSpecies} object.}

\item{tree}{Optional tree object. Supported inputs include \code{ape::phylo},
\code{tidytree::tbl_tree}, \code{treeio::treedata}, or a \code{ggtree} plot. A single
character value is treated as \code{tree_file}.}

\item{tree_file}{Optional single tree-file path. Newick files are read with
\code{ape::read.tree()} and Nexus files with \code{ape::read.nexus()}.}

\item{tree_plot}{Optional rectangular \code{ggtree} plot.}

\item{...}{Reserved for future tree reader options.}
}
\value{
The updated \code{SynSpecies} object.
}
\description{
\code{add_tree()} stores one tree representation on a \code{SynSpecies}. The input can
be a single tree file path, a tree object from \code{ape}, \code{tidytree}, or \code{treeio},
or a rectangular \code{ggtree} plot. If a new tree object is stored, any previous
stored tree plot is cleared; if a new tree plot is stored, any previous raw
tree object is cleared.
}
