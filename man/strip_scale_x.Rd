% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strip-scale-x.R
\name{strip_scale_x}
\alias{strip_scale_x}
\alias{strip_scale}
\title{X-only strip scale for gene-tag tracks}
\usage{
strip_scale_x(
  gene_gap_ratio = NULL,
  align = c("left", "right", "center"),
  homo_align = FALSE,
  species_specific_ratio = 0.5,
  secondary_homology_ratio = 0.75,
  species_ratio = NULL,
  collapse_contiguous_slot = TRUE,
  block_align = c("conserved", "left", "center", "right", "none"),
  guide = c("range", "none")
)

strip_scale(...)
}
\arguments{
\item{gene_gap_ratio}{Ratio of full gene visual width to intergenic gap
visual width. When \code{NULL}, the ratio is estimated from the densest track.}

\item{align}{Alignment for level-1, non-homology tracks with fewer genes than
the widest track.}

\item{homo_align}{\code{FALSE} for level-1 layout only, or a single character
reference track name for homology-aware layout. \code{TRUE} is not supported.}

\item{species_specific_ratio}{Visual width of a species-specific gene or
collapsed run relative to a homologous gene.}

\item{secondary_homology_ratio}{Visual width of a homologous off-track or
duplicate gene relative to a primary visible homologous gene.}

\item{species_ratio}{Deprecated alias for \code{species_specific_ratio}.}

\item{collapse_contiguous_slot}{Logical; when \code{TRUE}, contiguous
species-specific genes are compressed into one local run slot.}

\item{block_align}{Homology-mode track translation. \code{"conserved"} aligns the
highest-support reference homology block by median center offset, without
requiring query genes to appear in the same order. \code{"left"}, \code{"center"},
and \code{"right"} align each local track span to the reference span. \code{"none"}
leaves level-2 local coordinates untranslated.}

\item{guide}{Strip-scale x-axis guide. \code{"range"} draws a simple per-track
begin/end genomic bp range guide; \code{"none"} suppresses the custom guide.}

\item{...}{Arguments passed from the compatibility wrapper \code{strip_scale()} to
\code{strip_scale_x()}.}
}
\value{
A ggexon strip-scale-x specification, added to a plot with \code{+}.
}
\description{
\code{strip_scale_x()} normalizes gene-tag x coordinates so genes and intergenic
gaps occupy predictable visual widths. In homology mode, it can compress
species-specific local runs and translate tracks to align the most conserved
block against an explicit reference track.
}
