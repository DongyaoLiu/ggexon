% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strip-scale.R
\name{strip_scale}
\alias{strip_scale}
\title{Uniform gene-width scale for multi-track genomic plots}
\usage{
strip_scale(
  gene_gap_ratio = NULL,
  align = c("left", "right", "center"),
  homo_align = FALSE,
  species_ratio = NULL,
  collapse_contiguous_slot = TRUE,
  overlap = c("stack", "merge")
)
}
\arguments{
\item{gene_gap_ratio}{Ratio of gene visual width to intergenic gap width.
When \code{NULL} (the default), the ratio is derived from the densest
track's actual genomic proportions (median gene width divided by
median gap width).}

\item{align}{How tracks with fewer genes are positioned within the
shared x-axis. \code{"left"} packs genes to the left edge, \code{"right"} to
the right edge, \code{"center"} centres them.  Ignored when \code{homo_align}
is active (a warning is issued if set explicitly).}

\item{homo_align}{\code{FALSE} (default) for independent per-track ordering.
\code{TRUE} to auto-detect the reference species from homology annotations
and align homologous genes at the same visual x-position across all
tracks.  A character value (e.g. \code{"C. elegans N2"}) explicitly names
the reference species.}

\item{species_ratio}{Visual width ratio for species-specific genes
relative to homologous genes.  \code{NULL} (default) auto-scales each
gene proportionally to its genomic length relative to the median
reference gene length.  A numeric value (e.g. \code{0.5}) sets a fixed
ratio.}

\item{collapse_contiguous_slot}{When \code{TRUE} (default), a run of
contiguous species-specific genes between two anchored homology
blocks is compressed into a single visual slot.  When \code{FALSE},
each species-specific gene gets its own slot.}

\item{overlap}{How to handle overlapping genes within a track.
\code{"stack"} (default) assigns overlapping genes to separate y-levels
(like transcript stacking in \code{geom_exon}).  \code{"merge"} combines
overlapping genomic intervals into a single merged block.}
}
\value{
A ggexon strip-scale specification, added to the plot with \code{+}.
}
\description{
\code{strip_scale()} normalizes gene and intergenic region widths across
genomic tracks so that every gene occupies the same visual width and
every intergenic gap occupies the same visual width. The track with the
most genes determines the shared coordinate system; sparser tracks are
aligned within that space.
}
\details{
This scale is designed for \code{\link[=geom_genelabel]{geom_genelabel()}} layers and is mutually
exclusive with \code{\link[=scale_x_ggexon_genomic]{scale_x_ggexon_genomic()}}. It works best with
\code{facet_genomictree(scales = "fixed_x")} — the function will modify the
facet to use fixed x scales internally.
}
