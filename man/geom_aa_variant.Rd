% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-aa-variant.R
\name{geom_aa_variant}
\alias{geom_aa_variant}
\title{Annotate amino-acid variants on the exon structure}
\usage{
geom_aa_variant(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  species = NULL,
  chr = NULL,
  subset = NULL,
  annotation = NULL,
  genes = NULL,
  transcripts = NULL,
  strains = NULL,
  mutation = NULL,
  event_type = NULL,
  min_sample_count = NULL,
  protein_ranges = NULL,
  ref = NULL,
  exon_height = NULL,
  stem_height = NULL,
  y_offset = NULL,
  stem = NULL,
  spread = NULL,
  spread_threshold = NULL,
  curve_k = NULL,
  stem_colour = NULL,
  stem_linewidth = NULL,
  label_size = NULL,
  label_angle = NULL,
  label_gap = NULL,
  label_colour = NULL,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, stat, position, ..., na.rm, show.legend, inherit.aes}{Standard
ggplot2 layer arguments.}

\item{species}{Optional species/individual selector when plotting from a
\code{SynSpecies} object.}

\item{chr}{Optional chromosome name used to define the projection window.}

\item{subset}{Optional genomic window used to limit projected variants.}

\item{annotation}{Optional \code{SynProteinMutationAnnotation} layer name.}

\item{genes, transcripts}{Optional identifiers limiting the transcripts that
variants are projected onto.}

\item{strains, mutation, event_type, min_sample_count, protein_ranges, ref}{Optional variant filters forwarded to \code{\link[=query_protein_mutations]{query_protein_mutations()}}.}

\item{exon_height}{Exon rectangle height used to align markers with the exon
layer. Defaults to \code{0.8} to match \code{\link[=geom_exon]{geom_exon()}}.}

\item{stem_height}{Height of the lollipop stem above the exon top.}

\item{y_offset}{Additional vertical offset applied to every marker.}

\item{stem}{Logical; draw the connecting stem under each head.}

\item{spread}{Logical; when \code{TRUE} (default) heads are pushed apart
horizontally so crowded lollipops do not overlap, while each stem stays
anchored at the true codon position.}

\item{spread_threshold}{Minimum horizontal gap (in genomic units) enforced
between adjacent heads. When \code{NULL}, a value scaled to the data range is
used.}

\item{curve_k}{Sigmoid steepness of the curved stems linking each anchored
base to its (possibly spread) head.}

\item{stem_colour, stem_linewidth}{Stem styling.}

\item{label_size, label_angle, label_gap, label_colour}{Styling for optional
text labels. Labels are drawn at the spread head positions when a \code{label}
aesthetic is mapped, e.g. \code{aes(label = mutation)}.}
}
\value{
A ggplot2 layer using \code{\link{GeomAaVariant}}.
}
\description{
\code{geom_aa_variant()} draws protein-coordinate variants (for example \code{C316H}
at residue 316) at their genomic codon positions, as lollipop markers sitting
above the exon/intron model produced by \code{\link[=geom_exon]{geom_exon()}}. When the plot data is
a \code{SynIndividual} or \code{SynSpecies}, the layer resolves an attached
\code{SynProteinMutationAnnotation}, projects each variant onto its transcript's
CDS structure with \code{\link[=project_mutations_to_genome]{project_mutations_to_genome()}} (splice-aware, with codons
that cross an intron mapped to the exonic base), and aligns each marker with
the matching transcript row of the exon layer.
}
\details{
The resolved table exposes the variant metadata columns (such as \code{position},
\code{ref}, \code{alt}, \code{mutation}, and \code{sample_count}) so they can drive aesthetics,
e.g. \code{aes(fill = sample_count)} or \code{aes(colour = ref)}.

A plain data frame with \code{x}/\code{y} columns can also be supplied for full manual
control.
}
\seealso{
\code{\link[=project_mutations_to_genome]{project_mutations_to_genome()}}, \code{\link[=geom_exon]{geom_exon()}}, \code{\link[=geom_motif]{geom_motif()}}
}
