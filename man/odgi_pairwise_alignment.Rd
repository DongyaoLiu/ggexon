% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odgi.R
\name{odgi_pairwise_alignment}
\alias{odgi_pairwise_alignment}
\title{Convert an ODGI multiple alignment into a pairwise alignment}
\usage{
odgi_pairwise_alignment(
  x,
  query_individual,
  target_individual,
  name = NULL,
  individuals = NULL,
  odgi = NULL,
  python = NULL,
  file = NULL,
  metadata = list()
)
}
\arguments{
\item{x}{A \code{\link{SynMultiAlignment}} with \code{format = "odgi"}, an ODGI node-table
\code{data.frame}, a path to an ODGI node-table TSV, or a raw \code{.og} graph path.}

\item{query_individual}{Query-side individual identifier.}

\item{target_individual}{Target-side individual identifier.}

\item{name}{Optional pairwise alignment label. Defaults to
\code{"<alignment>__<query>__<target>"}.}

\item{individuals}{Optional individual mapping used when \code{x} is not already a
\code{SynMultiAlignment}. If named, the names must match ODGI path labels.}

\item{odgi}{Optional path to the \code{odgi} executable. Used when \code{x} is an
\code{.og} graph file.}

\item{python}{Optional path to the Python interpreter. Used when \code{x} is an
\code{.og} graph file.}

\item{file}{Optional source file to store on the returned object.}

\item{metadata}{Optional metadata list.}
}
\value{
A \code{\link{SynPairAlignment}} object with \code{format = "odgi"} and a cached
PAF-like pairwise table in its \code{data} slot.
}
\description{
Builds a PAF-like pairwise link table for one selected pair of individuals
from an ODGI node table, raw \code{.og} graph, or \code{\link{SynMultiAlignment}} with
\code{format = "odgi"}. The returned object is a \code{\link{SynPairAlignment}} with
\code{format = "odgi"}, so it can be added directly to a \code{\link{SynSpecies}} object
and consumed by \code{geom_nuclink()}.
}
