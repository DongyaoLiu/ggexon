# Retrieve the locus table from a SynLocusSet

Retrieve the locus table from a SynLocusSet

## Usage

``` r
locus_table(x)

# S4 method for class 'SynLocusSet'
locus_table(x)
```

## Arguments

- x:

  A `SynLocusSet` object.

## Value

A data frame with one row per locus window.
