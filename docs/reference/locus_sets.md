# List locus sets attached to a SynSpecies

List locus sets attached to a SynSpecies

## Usage

``` r
locus_sets(x)

# S4 method for class 'SynSpecies'
locus_sets(x)
```

## Arguments

- x:

  A `SynSpecies` object.

## Value

A named list of `SynLocusSet` objects.
