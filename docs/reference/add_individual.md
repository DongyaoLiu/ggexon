# Add SynIndividual objects to a SynSpecies object

Add SynIndividual objects to a SynSpecies object

## Usage

``` r
add_individual(x, individual, ...)
```

## Arguments

- x:

  A `SynSpecies` object.

- individual:

  A `SynIndividual` object.

- ...:

  Additional `SynIndividual` objects to add.

## Value

An updated `SynSpecies` object.
