# Retrieve an annotation layer from a SynIndividual

Retrieve an annotation layer from a SynIndividual

## Usage

``` r
get_annotation(x, name = NULL)
```

## Arguments

- x:

  A `SynIndividual` object.

- name:

  Annotation layer name. Defaults to the active annotation.

## Value

A `SynAnnotation` object.
