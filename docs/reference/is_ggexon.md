# Test whether an object is a ggexon plot

Test whether an object is a ggexon plot

## Usage

``` r
is_ggexon(x)
```

## Arguments

- x:

  An object to test.

## Value

`TRUE` when `x` is a `ggexon` plot object, otherwise `FALSE`.
